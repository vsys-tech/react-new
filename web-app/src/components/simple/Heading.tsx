import {HeadingProps} from "../../types/Heading.type.ts";

const Heading = ({children}: HeadingProps) => {

    return (
        <>
            {children}
        </>
    )

}


export default Heading;